@{
    ViewData["Title"] = "Manage Users";
    var userRoles = ViewBag.UserRoles as List<object>;
}

<div class="fade-in">
    <!-- Page Header -->
    <div class="row mb-4">
        <div class="col-12">
            <div class="d-flex justify-content-between align-items-center">
                <div>
                    <h2 class="mb-1"><i class="bi bi-people me-2 text-primary"></i>Manage Users</h2>
                    <p class="text-muted mb-0">View and manage all system users</p>
                </div>
                <div class="d-flex gap-2">
                    <a asp-action="CreateAdmin" class="btn btn-success">
                        <i class="bi bi-person-plus-fill me-1"></i>Add Admin
                    </a>
                    <a href="@Url.Action("Create", "Employees")" class="btn btn-primary">
                        <i class="bi bi-person-plus me-1"></i>Add Support Employee
                    </a>
                    <a href="@Url.Action("Index", "Admin")" class="btn btn-outline-secondary">
                        <i class="bi bi-arrow-left me-1"></i>Back to Dashboard
                    </a>
                </div>
            </div>
        </div>
    </div>

    <!-- Users Table -->
    <div class="row">
        <div class="col-12">
            <div class="card border-0 shadow-sm">
                <div class="card-header bg-primary text-white">
                    <h5 class="mb-0">
                        <i class="bi bi-table me-2"></i>All Users (@userRoles.Count)
                    </h5>
                </div>
                <div class="card-body p-0">
                    @if (userRoles != null && userRoles.Any())
                    {
                        <div class="table-responsive">
                            <table class="table table-hover mb-0">
                                <thead class="table-light">
                                    <tr>
                                        <th class="border-0 fw-semibold"><i class="bi bi-person me-1 text-primary"></i>Display Name</th>
                                        <th class="border-0 fw-semibold"><i class="bi bi-envelope me-1 text-primary"></i>Email</th>
                                        <th class="border-0 fw-semibold"><i class="bi bi-shield me-1 text-primary"></i>Role(s)</th>
                                        <th class="border-0 fw-semibold"><i class="bi bi-calendar me-1 text-primary"></i>Registration Date</th>
                                        <th class="border-0 fw-semibold"><i class="bi bi-check-circle me-1 text-primary"></i>Email Confirmed</th>
                                        <th class="border-0 fw-semibold"><i class="bi bi-gear me-1 text-primary"></i>Actions</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (dynamic userRole in userRoles)
                                    {
                                        var user = userRole.User as WebApplication2.Areas.Identity.Data.WebApplication2User;
                                        var roles = userRole.Roles as IList<string>;
                                        
                                        <tr>
                                            <td>
                                                <div class="d-flex align-items-center">
                                                    <div class="avatar-circle me-2">
                                                        @if (!string.IsNullOrEmpty(user.Displayname))
                                                        {
                                                            @user.Displayname.Substring(0, 1).ToUpper()
                                                        }
                                                        else
                                                        {
                                                            @user.Email.Substring(0, 1).ToUpper()
                                                        }
                                                    </div>
                                                    <div>
                                                        <strong>@(user.Displayname ?? "N/A")</strong>
                                                    </div>
                                                </div>
                                            </td>
                                            <td>@user.Email</td>
                                            <td>
                                                @if (roles != null && roles.Any())
                                                {
                                                    @foreach (var role in roles)
                                                    {
                                                        <span class="badge @GetRoleBadgeClass(role) me-1">@role</span>
                                                    }
                                                }
                                                else
                                                {
                                                    <span class="badge bg-secondary">No Role</span>
                                                }
                                            </td>
                                            <td>
                                                @if (user.LockoutEnd.HasValue)
                                                {
                                                    @user.LockoutEnd.Value.ToString("yyyy-MM-dd")
                                                }
                                                else
                                                {
                                                    <span class="text-muted">N/A</span>
                                                }
                                            </td>
                                            <td>
                                                @if (user.EmailConfirmed)
                                                {
                                                    <span class="badge bg-success">
                                                        <i class="bi bi-check-circle"></i> Confirmed
                                                    </span>
                                                }
                                                else
                                                {
                                                    <span class="badge bg-warning">
                                                        <i class="bi bi-exclamation-triangle"></i> Pending
                                                    </span>
                                                }
                                            </td>
                                            <td>
                                                @{
                                                    var currentUserId = ViewContext.HttpContext.User.FindFirst(System.Security.Claims.ClaimTypes.NameIdentifier)?.Value;
                                                    var isCurrentUser = currentUserId == user.Id;
                                                }
                                                @if (!isCurrentUser)
                                                {
                                                    <a asp-action="DeleteUser" asp-route-id="@user.Id" 
                                                       class="btn btn-outline-danger btn-sm" 
                                                       title="Delete User"
                                                       onclick="return confirm('Are you sure you want to delete this user?')">
                                                        <i class="bi bi-trash"></i>
                                                    </a>
                                                }
                                                else
                                                {
                                                    <span class="text-muted small">Current User</span>
                                                }
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    }
                    else
                    {
                        <div class="text-center py-5">
                            <i class="bi bi-people display-1 text-muted"></i>
                            <h4 class="text-muted mt-3">No users found</h4>
                            <p class="text-muted">There are currently no users in the system.</p>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .avatar-circle {
        width: 40px;
        height: 40px;
        border-radius: 50%;
        background-color: #007bff;
        color: white;
        display: flex;
        align-items: center;
        justify-content: center;
        font-weight: bold;
        font-size: 16px;
    }
</style>

@functions {
    string GetRoleBadgeClass(string role)
    {
        return role switch
        {
            "Admin" => "bg-danger",
            "Support Employee" => "bg-info",
            "Farmer" => "bg-success",
            _ => "bg-secondary"
        };
    }
}